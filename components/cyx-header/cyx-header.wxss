.header {
  height: 120rpx;
  border: 1px solid var(--themeColor);
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx;
}

.header .logo {
  height: 115rpx;
}

.header .logo .pic {
  height: 100%;
}

.header .search {
  color: var(--themeColor);
}
